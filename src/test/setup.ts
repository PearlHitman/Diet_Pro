// Global test setup — runs once before the test suite.
//
// This batch of tests covers pure logic (no React rendering), so nothing
// is needed here yet. When you add component render tests later:
//   1. npm i -D @testing-library/react @testing-library/jest-dom jsdom
//   2. uncomment the line below
//   3. import { render, screen } from '@testing-library/react' in the test
//
// import '@testing-library/jest-dom/vitest';

export {};
